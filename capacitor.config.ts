import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.j4mes.mclaren.app',
  appName: 'ionic-swiper',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
