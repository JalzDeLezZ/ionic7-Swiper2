import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.jmclaren.ionic-swiper',
  appName: 'ionic-swiper',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
